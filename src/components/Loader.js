import React, { PureComponent } from 'react';

class Loader extends PureComponent {
    render() {
        return (
            <div>
                Loading...
            </div>
        );
    }
}

export default Loader;